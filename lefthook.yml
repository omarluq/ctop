# Lefthook configuration for Ctop
# https://github.com/evilmartians/lefthook
pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.cr"
      run: task format
      stage_fixed: true
    ameba:
      glob: "*.cr"
      run: task ameba
    yamlfmt:
      glob: "*.{yml,yaml}"
      run: yamlfmt {staged_files}
      stage_fixed: true
